<%- include("partials/header") %> 

<table style="margin-top: 135px;" class="table align-middle mb-0 bg-dark">
    <thead class="bg-dark">
      <tr>
        <th>User Name</th>
        <th>Email</th>
        <th>Phone</th>
        <th>status</th>
        <th>Actions</th>
      </tr>
    </thead>
    <tbody>
      <% for(let i=0;i<locals.UserData.length;i++){ %>
      <tr>
        
          <td><%= UserData[i].username %></td>
          <!-- <div class="d-flex align-items-center">
            <img
                src="https://mdbootstrap.com/img/new/avatars/8.jpg"
                alt=""
                style="width: 45px; height: 45px"
                class="rounded-circle"
                />
            <div class="ms-3">
              <p class="fw-bold mb-1">John Doe</p>
              <p class="text-muted mb-0">john.doe@gmail.com</p>
            </div>
          </div> -->

        </td>
        <td>
          <!-- <p class="fw-normal mb-1">Software engineer</p>
          <p class="text-muted mb-0">IT department</p> -->
          <%= UserData[i].email %>
        </td>
        <td>
       
          <%= UserData[i].phone %>
         
      
        </td>
        <td>
            <% if(UserData[i].isBlocked==0){ %> 
              <button onclick="deleteProduct('<%= UserData[i].isBlocked  %>','<%= UserData[i]._id  %>')" type="button" class="btn btn-success rounded-pill">Active</button>
             
            
                <% } else{ %>  
              
                  <button onclick="location.href='/admin/Active?status=<%= UserData[i].isBlocked  %>&id=<%= UserData[i]._id  %>  '"  type="button" class="btn btn-danger rounded-pill">Deactivated</button>
          
                  <% } %>
        </td>
      
        <td>
          <button type="button" class="btn btn-link btn-sm btn-rounded">
            Edit
          </button>
        </td>                   
      </tr>
      <% } %>
    </tbody>
  </table>
  <script src="https://unpkg.com/sweetalert/dist/sweetalert.min.js"></script>
<%- include("partials/footer") %> 